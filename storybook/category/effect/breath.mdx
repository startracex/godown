import { Meta, Title, Subtitle, Description, Primary, Controls, Stories } from "@storybook/blocks";

import { ReduxIcon } from "@storybook/icons";

<Meta isTemplate />
<Title />
<Description />
<Primary />
<Controls />

If you see multiple colors light up at the same time.

Due to computed style update completed, but the browser did not re render.

Click <ReduxIcon style={{ width:"18px", height: "18px", verticalAlign: "text-top" }} /> in toolbar to fresh style.
